import styled from 'styled-components';

export const MainContainerStyled = styled('div')({
  display: 'flex',
  gap: '24px',
  marginTop: 16,
});

export const AvatarSectionStyled = styled('div')({});

export const InfoSectionStyled = styled('div')({
  display: 'flex',
  flexDirection: 'column',
});

export const ButtonsSectionStyled = styled('div')({
  display: 'flex',
  marginLeft: 'auto',
  marginBottom: 'auto',
  height: 36,
  gap: 6,
});

export const NameSectionStyled = styled('div')({
  display: 'flex',
  flexDirection: 'column',
});
export const EmployeeNumberContainerStyled = styled('div')({
  marginTop: '16px',
  display: 'flex',
  flexDirection: 'column',
});

export const EmployeeNumberStyled = styled('div')({
  display: 'flex',
});

